<div class="row">
    <div class="col-sm-9 center">
        {{id}}. {{burger_name}}
    </div>
    <div class="col-sm-3">
        {{#if devoured}} <button class="eatBurger" data-id="{{id}}">
            🍽
        </button>{{/if}}
    </div>
</div>